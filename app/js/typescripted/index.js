var inputs = $('.inputField');
var amount = $('#amount');
var salary = $('#expectedSalary');
var repayment = $('#repayment');
inputs.addEventListener('input', function (e) {
    if (e.target == amount) {
    }
    else if (e.target == salary) {
    }
    else if (e.target == repayment) {
    }
});
